plugins {
    id 'fabric-loom' version '1.11.8'
    id 'java'
}

group = 'com.logan'
version = '1.21.1-1.0.0'
base {
    archivesName = 'originswildfirecompat-fabric'
}

loom {
    mods {
        originswildfirecompat {
            sourceSet sourceSets.main
        }
    }
}

repositories {
    mavenCentral()
    maven {
        name = 'Fabric'
        url = 'https://maven.fabricmc.net/'
    }
    maven {
        name = 'Modrinth'
        url = 'https://api.modrinth.com/maven'
    }
}

dependencies {
    minecraft 'com.mojang:minecraft:1.21.1'
    mappings 'net.fabricmc:yarn:1.21.1+build.3:v2'
    modImplementation 'net.fabricmc:fabric-loader:0.19.5'
    modImplementation 'net.fabricmc.fabric-api:fabric-api:0.116.17+1.21.1'

    // Origins Fabric 1.13.0-pre.3+mc.1.21.1 (pre-release, only option for 1.21.1)
    modImplementation 'maven.modrinth:3BeIrqZR:fcGzZVGn'

    // Pehkui Fabric 3.8.3 (covers 1.21.1)
    modImplementation 'maven.modrinth:t5W7Jfwy:6fzxx6SC'

    // Wildfire's Female Gender Mod Fabric 3.2.1+1.21 (covers 1.21/1.21.1)
    modImplementation 'maven.modrinth:bcOsQHnC:kJmjQvAS'

    // Calio 1.14.0 (transitive dep of Origins/Apoli for compiling; runtime comes via Origins' JiJ)
    // Jar extracted from Apoli's JiJ, since Apace doesn't publish Calio 1.14 to a public Maven
    modImplementation files('libs/calio-1.14.0+mc.1.21.x.jar')
}

processResources {
    inputs.property 'version', project.version
    filesMatching('fabric.mod.json') {
        expand version: project.version
    }
}

tasks.withType(JavaCompile).configureEach {
    it.options.release = 21
}
